spring:
  application:
    name: user
  cloud:
    config:
      name: config
      profile: dev
      label: dev
      uri: http://localhost:8888/
  redis:
    host: 127.0.0.1
    port: 6379

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8081/eureka/
server:
  port: 8082
